# Local-only project rule

- This project must run and be tested only on localhost.
- Never deploy or publish this project with Sites or any other hosting provider.
- Never open or run the application inside Codex.
- Do not add a `project_id` to `.openai/hosting.json`.
